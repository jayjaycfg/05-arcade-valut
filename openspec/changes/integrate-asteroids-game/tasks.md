## 1. Add the catalog entry

- [x] 1.1 Add a new entry (`id: 'asteroids'`) to `GAMES` in `lib/games.ts` with title/short/long/cat/cover/color/best/plays fields matching the existing `Game` type (copy adapted from `resources/started-games/02-asteroids/README.md`), and verify it shows up on the game library screen and has its own working detail page at `/juegos/asteroids`, distinct from the existing `rocas` entry.

## 2. Port the engine into a mountable factory

- [x] 2.1 Create `lib/games/asteroids/engine.ts`, copying the gameplay code from `resources/started-games/02-asteroids/game.js` (constants, `Bullet`/`Asteroid`/`Ship`/`Particle`/`PowerUp` classes, `wrap`/`dist`/`rand`/`randInt` utils) unchanged, and verify it compiles under the project's TypeScript config (`npm run build` or `tsc --noEmit`).
- [ ] 2.2 Wrap the module-level state/functions (`ship`, `bullets`, `asteroids`, `particles`, `powerUps`, `score`, `lives`, `level`, `state`, `initGame`, `nextLevel`, `update`, `draw`, `loop`) inside a `createAsteroidsGame(canvas: HTMLCanvasElement, callbacks: { onState, onGameOver }): AsteroidsController` factory, replacing the top-of-file `document.getElementById('canvas')` with the `canvas` parameter, and verify by writing a throwaway script/test that calls the factory twice on two different canvases without either instance's state leaking into the other.
- [ ] 2.3 Remove the canvas-drawn HUD and "GAME OVER" overlay (`drawHUD`, `drawOverlay`, and their calls in `draw()`) since the host page renders that chrome, and verify by visual check that the canvas now only renders ship/asteroids/bullets/particles/power-ups.
- [ ] 2.4 Call `callbacks.onState({ score, lives, level, gameOver })` once per frame at the end of `update()`, and `callbacks.onGameOver(score)` the frame `state` transitions to `'gameover'`, and verify via a manual test harness (temporary console.log or unit test) that the callback fires with the expected shape as score/lives/level change during play.
- [ ] 2.5 Implement `pause()`/`resume()` via a `running` boolean checked at the top of the loop's `update()` call and in the `keydown` handler, `reset()` by re-running the `initGame()`-equivalent logic in place, and `destroy()` by cancelling the `requestAnimationFrame` handle and removing the `window` `keydown`/`keyup` listeners added on construction; verify by calling each method from a scratch script and confirming (a) paused sessions stop advancing and ignore input, (b) reset returns score/lives/level to initial values, (c) destroy leaves no listeners registered (`getEventListeners`-style check or listener-count assertion) and no further RAF callbacks firing.

## 3. React integration

- [ ] 3.1 Create `components/games/AsteroidsGame.tsx` (`'use client'`) that renders an `800x600` `<canvas>`, creates the engine via `createAsteroidsGame` in a `useEffect` on mount, exposes an imperative handle or prop-driven controller (matching how `GamePlayer` needs to call pause/resume/reset), and calls `destroy()` in the effect's cleanup; verify by mounting it in isolation (temporary route or Storybook-less manual page) and confirming Strict Mode's double-invoke in dev does not produce two running loops (check via a single `console.log` in the RAF loop, or a frame counter that doesn't double-tick).
- [ ] 3.2 In `components/GamePlayer.tsx`, branch on `game.id === 'asteroids'`: render `<AsteroidsGame>` wired to the engine's `onState`/`onGameOver` callbacks feeding the existing `score`/`lives`/`level`/`over` state, instead of the simulated `setInterval` ticker; verify by starting the `asteroids` game and confirming the HUD score/lives/level move only in response to actual gameplay (destroying asteroids, losing the ship), not on a timer.
- [ ] 3.3 Wire the existing "PAUSA"/"REANUDAR" button to call the engine's `pause()`/`resume()` for the `asteroids` branch (in addition to the existing `paused` state toggle that drives the overlay), and verify by pausing mid-game: asteroids/ship freeze on screen and arrow/space input has no effect until resumed.
- [ ] 3.4 Make `onGameOver` (last life lost) set the same `over` state the "FIN" button sets today, seeded with the engine's reported final score, and verify by playing until all lives are lost and confirming the existing game-over modal appears with the correct score, without pressing "FIN".
- [ ] 3.5 Make "JUGAR DE NUEVO" call the engine's `reset()` for the `asteroids` branch (in addition to the existing `restart()` state reset) and verify by finishing a game, clicking replay, and confirming a fresh session starts (score 0, 3 lives, level 1, ship centered) without navigating away.
- [ ] 3.6 Confirm unmounting the play screen (clicking "SALIR" or navigating away mid-session) tears the engine down cleanly — verify by checking, after navigating away and back into `asteroids` several times, that only one set of keyboard listeners/one RAF loop is ever active (no runaway asteroid speed-up or duplicated input response from stacked listeners).

## 4. Regression check on other games

- [ ] 4.1 Verify `rocas` and every other non-`asteroids` catalog game (`bloque-buster`, `caida`, `serpentina`, `gloton`, `invasores`, `ranaria`, `duelo-pixel`) still show the original simulated score ticker, pause/resume, and end-of-game score submission unchanged, by spot-checking `rocas` plus at least one other such game end-to-end.
- [ ] 4.2 Run `npm run lint` and `npm run build`, and verify both complete with no new errors introduced by this change.
